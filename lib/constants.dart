import 'package:flutter/material.dart';

// COLOR
const kColorPrimary = Colors.blue;
const kColorPrimaryLight = Color(0xFFF1E6FF);
const kColorPrimaryGradient = LinearGradient(
  begin: Alignment.topLeft,
  end: Alignment.bottomRight,
  colors: [
    Color(0xFFFFA53E),
    Color(0xFF6F35A5)
  ],
);
const kColorSecondary = Color(0xFF979797);
const kColorText = Color(0xFF757575);

// ANIMATION
const kAnimationDuration = Duration(milliseconds: 200);